# BasketGuard Artifacts

Local runtime artifacts can be written here while developing.

The ingestion persistence command defaults raw HTML snapshots to:

```text
artifacts/raw_snapshots/
```

Snapshot payloads are generated data and should not be committed.
